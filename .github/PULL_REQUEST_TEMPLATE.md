## What does this change?

<!-- A short description of what changed and why. Link the issue it addresses, if any (e.g. "Closes #12"). -->

## How was this tested?

<!-- e.g. "Added a unit test for X", "Manually verified the sign-in flow in a browser" -->

## Checklist

- [ ] `npm run lint` passes
- [ ] `npx tsc --noEmit -p tsconfig.app.json` passes
- [ ] `npm test` passes
- [ ] `npm run build` passes
- [ ] Every commit has a `Signed-off-by` trailer (`git commit -s`) — see [CONTRIBUTING.md](../CONTRIBUTING.md#commit-sign-off-dco)
- [ ] Added/updated a migration under `supabase/migrations/` if this changes the database schema
- [ ] Added or updated tests for any behavior change
